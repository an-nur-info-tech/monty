#include "monty.h"
/**
 * stack - changes to LIFO (stack uses LIFO rules)
 * @stack: monty stack
 * @line_number: line number of file
 */
void stack(stack_t **stack, unsigned int line_number)
{
	UNUSED(stack);
	UNUSED(line_number);
	StackQueue = 1;
}
