#include "monty.h"
/**
 * queue - changes to FIFO (queue uses FIFO rules)
 * @stack: monty stack
 * @line_number: line number of file
 */
void queue(stack_t **stack, unsigned int line_number)
{
	UNUSED(stack);
	UNUSED(line_number);
	StackQueue = 0;
}
